Active task: .trellis/tasks/08-10-exp2-initial-calibration

You are an independent, skeptical benchmark/dataset reviewer. READ-ONLY: do not
edit files, create tasks, download images, or execute cases. Treat existing
cohorts as proposals rather than commitments.

Audit which datasets and splits best serve the project goal: evidence-driven
improvement of a repo-agnostic bugfix harness, not leaderboard optimization.
Compare SWE-bench Verified, SWE-bench-Live/MultiLang, and SWE-bench Pro using
official current sources and the pinned local protocol.

Specifically challenge:

- two calibration cases from repos excluded from formal data;
- ten Verified formal cases, one per repository, in adaptive 2/3/5 slices;
- six sealed Live cases;
- Pro deferred to a separate replication;
- instance-scoped image acquisition rather than full download.

Deliver construct-validity risks, contamination/saturation/floor risks,
repository/task/language coverage, scorer maturity, minimum useful roles and
denominators, and exact claims each design supports. End with a single dataset
architecture and a verdict: ADOPT, REVISE, or REJECT.
